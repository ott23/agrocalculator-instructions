1. Установка Oracle Java 8 (если не установлена) [в квадратных скобках Java 10]

sudo add-apt-repository ppa:webupd8team/java
[sudo add-apt-repository ppa:linuxuprising/java]

sudo apt-get update

sudo apt-get install oracle-java8-installer -y
[sudo apt install oracle-java10-installer -y]

Путь к jvm: 
/usr/lib/jvm/java-8-oracle
[usr/lib/jvm/java-10-oracle]

sudo sh -c " echo 'JAVA_HOME=\"/usr/lib/jvm/java-8-oracle\"' >> /etc/environment"
[sudo sh -c " echo 'JAVA_HOME=\"/usr/lib/jvm/java-10-oracle\"' >> /etc/environment"]

source /etc/environment

2. Установить tomcat

sudo mkdir /opt/tomcat-9
cd ~
wget http://mirror.linux-ia64.org/apache/tomcat/tomcat-9/v9.0.10/bin/apache-tomcat-9.0.10.tar.gz
//wget http://ftp.wayne.edu/apache/tomcat/tomcat-9/v9.0.7/bin/apache-tomcat-9.0.7.tar.gz
sudo tar -xzvf apache-tomcat-*.tar.gz -C /opt/tomcat-9 --strip-components=1
rm apache-tomcat-9*.tar.gz

3. Создать пользователя tomcat с группой tomcat

sudo groupadd tomcat
sudo useradd -s /bin/false -g tomcat -d /opt/tomcat-9 tomcat
sudo chown -R tomcat:tomcat /opt/tomcat-9

4. Настройка tomcat на 80 порт

sudo apt install authbind -y
sudo touch /etc/authbind/byport/80
sudo chmod 500 /etc/authbind/byport/80
sudo chown tomcat:tomcat /etc/authbind/byport/80
sudo sed -i 's/8080/80/g' /opt/tomcat-9/conf/server.xml

sudo nano /opt/tomcat-9/bin/setenv.sh

	CATALINA_OPTS="-Djava.net.preferIPv4Stack=true"

ctrl+O, Enter, ctrl+X

sudo nano /opt/tomcat-9/bin/startup.sh (закомментить строку, рядом вписать новую)

	exec authbind --deep "$PRGDIR"/"$EXECUTABLE" start "$@"
	#exec "$PRGDIR"/"$EXECUTABLE" start "$@"

ctrl+O, Enter, ctrl+X

5. Создать сервис

sudo nano /etc/systemd/system/tomcat.service

	[Unit]
	Description=Tomcat Container
	After=network.target

	[Service]
	Type=forking

	Environment=JAVA_HOME=/usr/lib/jvm/java-8-oracle/jre
	Environment=CATALINA_PID=/opt/tomcat-9/tomcat9.pid
	Environment=CATALINA_HOME=/opt/tomcat-9
	Environment=CATALINA_BASE=/opt/tomcat-9

	ExecStart=/opt/tomcat-9/bin/startup.sh
	ExecStop=/opt/tomcat-9/bin/shutdown.sh

	User=tomcat
	Group=tomcat
	RestartSec=10
	Restart=always

	[Install]
	WantedBy=multi-user.target

ctrl+O, Enter, ctrl+X

6. Для удобства создать ссылку на webapps

sudo chmod -R 777 /opt/tomcat-9/webapps
cd ~
sudo ln -s /opt/tomcat-9/webapps webapps

7. Запустить сервис

sudo systemctl daemon-reload
sudo systemctl enable tomcat.service
sudo systemctl restart tomcat.service

8. Скопировать ROOT или ROOT.war в ~/webapps
